import { Link, NavLink } from 'react-router-dom';
import { Menu, Phone } from 'lucide-react';
import { useState } from 'react';
import { company, nav } from '../data/site';

export default function Navbar(){const[open,setOpen]=useState(false);return <>
<header className="sticky top-0 z-50 bg-white/90 backdrop-blur border-b border-slate-100"><div className="container flex items-center justify-between py-3"><Link to="/" className="flex items-center gap-3 font-black text-[#0B2D5B]"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-[#0B2D5B] text-white">S</span><span className="leading-tight">Spectralink<br/><small className="font-semibold text-slate-500">Global Logistics</small></span></Link><nav className="hidden lg:flex items-center gap-6 text-sm font700">{nav.map(([label,path])=><NavLink key={path} to={path} className={({isActive})=>isActive?'text-[#FF8A00]':'text-slate-700 hover:text-[#0B2D5B]'}>{label}</NavLink>)}</nav><div className="hidden md:flex gap-3"><a href={`tel:${company.phone}`} className="btn btn-secondary"><Phone size={17}/>Call</a><Link className="btn btn-primary" to="/contact">Get Free Quote</Link></div><button aria-label="Open menu" onClick={()=>setOpen(!open)} className="lg:hidden"><Menu/></button></div>{open&&<div className="container lg:hidden pb-4 grid gap-3">{nav.map(([label,path])=><Link onClick={()=>setOpen(false)} key={path} to={path} className="py-2 border-b">{label}</Link>)}</div>}</header><div className="mobile-actions"><a className="btn btn-secondary flex-1" href={`tel:${company.phone}`}>Call Now</a><a className="btn btn-primary flex-1" href={`https://wa.me/${company.whatsapp}`}>WhatsApp</a></div></>}
